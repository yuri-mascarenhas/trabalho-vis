class Config {
  constructor(width, height, margin, colors, labels) {
    this.width = width;
    this.height = height;
    this.margin = margin;
    this.colors = colors;
    this.labels = labels;
  }
}
